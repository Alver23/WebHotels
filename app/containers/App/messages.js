// Dependencies
import { defineMessages } from 'react-intl';

export default defineMessages({
  itemGetError: {
    id: 'WebHotels.containers.App.itemGetError',
    defaultMessage: 'An error occurred during attempt to obtain item json from server. Please, reload the page (CTRL+R).',
  },
});
